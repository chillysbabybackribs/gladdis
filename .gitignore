# Dependencies
node_modules/

# Electron/Vite build output
out/
dist/

# Runtime/debug artifacts
pipeline-runs/
*.log
.claude/*.lock
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Per-workspace Gladdis runtime state (auto-dream config, history,
# migration backups, telemetry). `memory.json` is intentionally tracked
# so dreamer changes show up in diffs; the rest is user-specific.
.gladdis/dream-auto.json
.gladdis/dream-history.json
.gladdis/memory-usage.jsonl
.gladdis/memory.v1.backup.json
.gladdis/memory.next.json
.gladdis/memory.next.diff.json

# Local environment
.env
.env.*
!.env.example

# OS/editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

# Codex skills (dev-only, do not commit)
.codex/skills/
