/** Root shell chrome — window, UI WebContentsView, and boot splash. */
export const SHELL_BACKGROUND = '#181818'

/** Dark native backing for the embedded WebContentsView. */
export const BROWSER_VIEW_BACKGROUND = '#1e1e1e'
